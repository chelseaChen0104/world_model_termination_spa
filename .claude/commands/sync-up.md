---
description: Push local code → AutoDL (code-only, excludes data/outputs/logs)
---

Run `bash scripts/sync-up.sh` from the repo root. Show the rsync output, then briefly confirm what changed (or "no changes" if nothing transferred).
